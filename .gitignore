dist/
# exceto o arquivo que a Vercel serve: é ele que vai pro ar, e versioná-lo
# deixa o deploy reprodutível sem depender do build rodar lá.
!dist/site/
!dist/site/index.html
!dist/site/api/
!dist/site/api/tarefas.mjs
node_modules/
.DS_Store

# Fotos que os testes tiram para eu conferir a tela. São conferência, não
# fonte: a que vale está sempre a um `node teste/<x>.mjs` de distância, e
# versionar imagem grande a cada rodada só engorda o repositório.
teste/*.png
